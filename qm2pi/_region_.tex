\message{ !name(qm2pi.rho.presentation.tex)}
\message{ !name(qm2pi.rho.presentation.tex) !offset(13) }
\begin{mathpar}
  \inferrule* [lab=summation] {} {{M,N} \bc \pzero \;|\; x.A \;|\; M+N }
  \and
  \inferrule* [lab=agent] {} {{A} \bc (\vec{x})P \;| \; \clift{\vec{P}}}
  \and
  \inferrule* [lab=process] {} {{P,Q} \bc N \;| \;P|Q \;|\; \dropn{x}}
  \and
  \inferrule* [lab=name] {} {{x,y} \bc \quotep{P}}
\end{mathpar} 
\message{ !name(qm2pi.rho.presentation.tex) !offset(-12) }
